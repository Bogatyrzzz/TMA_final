# 🦸 LifeQuest Hero - Telegram Mini App

**Геймифицируй свою жизнь с AI-героем!**

## 📋 Описание проекта

LifeQuest Hero - это Telegram Mini App для геймификации реальной жизни. Создай своего AI-героя, выполняй квесты, прокачивай характеристики и достигай целей!

### ⭐ Основные функции:

- 🦸 **AI-Аватар**: Генерация уникального героя на основе твоего селфи (стиль Captain America)
- 💪 **3 Ветки развития**: Power (Сила), Stability (Стабильность), Longevity (Долголетие)
- 📈 **Система прокачки**: 6 характеристик (Сила, Здоровье, Интеллект, Ловкость, Уверенность, Стабильность)
- 🎯 **Квесты**: Ежедневные задания для получения XP
- 🏆 **Цели**: Устанавливай и достигай реальных целей
- 🌟 **PRO подписка**: Доступ ко всем веткам одновременно
- 🤖 **Telegram Bot**: Ежедневные напоминания о квестах

## 🏗️ Технический стек

### Frontend:
- React 19
- Tailwind CSS
- shadcn/ui
- Telegram Web App SDK (@twa-dev/sdk)
- Framer Motion (анимации)
- Axios

### Backend:
- FastAPI (Python)
- Supabase (PostgreSQL)
- python-telegram-bot

## 🚀 Настройка проекта

### Шаг 1: Настройка Supabase

1. Открой Supabase Dashboard: https://supabase.com/dashboard
2. Перейди в свой проект: https://supabase.com/dashboard/project/ppzuwsmexjmjgrdylcvw
3. Открой **SQL Editor** в левом меню
4. Создай новый запрос и выполни SQL скрипт из файла: `/app/backend/supabase_schema.sql`

**Важно:** Этот скрипт создаст все необходимые таблицы, индексы, функции и загрузит квесты.

### Шаг 2: Настройка Telegram Bot

Бот уже создан с токеном: `8087615492:AAGuX0QnWtkpoQBuOYHzb0EqF9qrZ4N-f-Q`

**Настройка Mini App:**

1. Открой @BotFather в Telegram
2. Отправь команду `/setmenubutton`
3. Выбери своего бота
4. Выбери тип: **web_app**
5. Введи название: **LifeQuest Hero**
6. Введи URL: `https://your-backend-url`

### Шаг 3: Настройка n8n для генерации аватаров

**Нужно создать workflow в n8n с webhook для генерации AI-аватаров.**

#### Пример workflow:

1. **Webhook Node** - принимает данные:
   - `user_id` - ID пользователя
   - `tg_id` - Telegram ID
   - `selfie_url` - URL селфи (опционально)
   - `branch` - ветка развития (power/stability/longevity)
   - `gender` - пол (male/female)
   - `age` - возраст
   - `level` - текущий уровень

2. **HTTP Request Node** - вызов Gemini Nano Banana для генерации:
   - Промпт на основе данных пользователя
   - Стиль: 3D cartoon superhero (Captain America style)
   - Возвращает URL изображения

3. **HTTP Request Node** - отправка результата обратно:
   - POST `{BACKEND_URL}/api/webhooks/avatar-generated`
   - Body: `{ "user_id": "...", "avatar_url": "...", "level": 1 }`

**После создания workflow:**
- Скопируй URL webhook
- Добавь в `/app/backend/.env`: `N8N_WEBHOOK_URL=<твой_webhook_url>`
- Перезапусти backend: `sudo supervisorctl restart backend`

### Шаг 4: Запуск сервисов

Все сервисы уже запущены через supervisor:

```bash
# Проверить статус
sudo supervisorctl status

# Перезапустить при необходимости
sudo supervisorctl restart backend
sudo supervisorctl restart frontend

# Логи
tail -f /var/log/supervisor/backend.*.log
tail -f /var/log/supervisor/frontend.*.log
```

### Шаг 5: Запуск Telegram бота (опционально)

Бот для daily reminders можно запустить отдельно:

```bash
cd /app/backend
python bot.py
```

## 📂 Структура проекта

```
/app/
├── backend/
│   ├── server.py              # FastAPI backend
│   ├── bot.py                 # Telegram bot
│   ├── supabase_client.py     # Supabase клиент
│   ├── supabase_schema.sql    # SQL схема БД
│   ├── requirements.txt       # Python зависимости
│   └── .env                   # Переменные окружения
│
├── frontend/
│   ├── src/
│   │   ├── App.js            # Главный компонент
│   │   ├── components/
│   │   │   ├── Onboarding.js   # Онбординг (3 шага)
│   │   │   ├── HomeScreen.js   # Главный экран
│   │   │   └── ui/             # shadcn/ui компоненты
│   │   └── lib/
│   │       ├── api.js          # API клиент
│   │       ├── telegram.js     # Telegram SDK утилиты
│   │       └── supabase.js     # Supabase клиент
│   ├── package.json
│   └── .env
│
└── README.md
```

## 🎮 Как играть

### Для пользователей:

1. **Открой бота** в Telegram и нажми `/start`
2. **Запусти Mini App** через кнопку "🦸 Запустить LifeQuest Hero"
3. **Пройди онбординг**:
   - Укажи возраст и пол
   - Выбери ветку развития
   - Поставь цель
4. **Выполняй квесты** каждый день и получай XP
5. **Прокачивай уровень** и характеристики
6. **Достигай целей** и становись сильнее!

### Система прогрессии:

- **Уровни**: С 1 по 50 (максимум)
- **XP формула**: `100 * 1.05^(level-1)`
- **Квесты**: 15-20 XP за базовые, 25-50 XP за сложные
- **Статы**: +1 ко всем при level up, +2-3 к специфичным ветки

### PRO подписка:

- Активация всех 3 веток одновременно
- Больше квестов и XP
- Эксклюзивные задания

## 🔧 API Endpoints

### Users:
- `POST /api/users/register` - Регистрация пользователя
- `GET /api/users/{tg_id}` - Получить пользователя
- `POST /api/users/{tg_id}/onboarding` - Завершить онбординг
- `GET /api/users/{tg_id}/progress` - Получить прогресс

### Quests:
- `GET /api/users/{tg_id}/quests` - Получить квесты
- `POST /api/users/{tg_id}/quests/complete` - Завершить квест

### PRO:
- `POST /api/users/{tg_id}/pro/activate` - Активировать PRO
- `POST /api/users/{tg_id}/branches/add` - Добавить ветку

### Webhooks:
- `POST /api/webhooks/avatar-generated` - Webhook от n8n

## 🐛 Отладка

### Backend не запускается:

```bash
# Проверь логи
tail -f /var/log/supervisor/backend.err.log

# Проверь переменные окружения
cat /app/backend/.env

# Ручной запуск для отладки
cd /app/backend
uvicorn server:app --host 0.0.0.0 --port 8001 --reload
```

### Frontend не компилируется:

```bash
# Проверь логи
tail -f /var/log/supervisor/frontend.err.log

# Переустанови зависимости
cd /app/frontend
yarn install

# Ручной запуск
yarn start
```

### Supabase ошибки:

```bash
# Проверь правильность учетных данных
echo $REACT_APP_SUPABASE_URL
echo $REACT_APP_SUPABASE_KEY

# Проверь, что таблицы созданы
# Открой Supabase Dashboard -> Table Editor
```

## 📊 База данных

### Основные таблицы:

- **users** - Пользователи (tg_id, stats, branches, avatar_url)
- **progress** - Прогресс (level, xp, goal)
- **quests** - Шаблоны квестов
- **user_quests** - Завершенные квесты
- **transactions** - Платежные транзакции
- **avatar_generations** - История генерации аватаров

### Функции:

- `add_xp_and_check_level()` - Добавить XP и проверить level up
- `update_stats_on_levelup()` - Обновить статы при прокачке

## 🚀 Деплой (для production)

### Vercel (Frontend):
```bash
vercel --prod
```

### Railway (Backend + Bot):
```bash
# Connect to GitHub
# Deploy from main branch
```

## 🎨 Дизайн

### Цветовая палитра:
- Primary: `#FF6B35` (оранжевый)
- Secondary: `#4ECDC4` (циан)
- Background: `#0F0F23` (темно-синий)
- Accent: `#1a1a2e` (темный)

### Стиль аватаров:
- 3D мультяшный
- Героический (Captain America style)
- Яркие цвета
- Прогрессия: от новичка до супергероя

## 📝 TODO / Backlog

- [ ] Интеграция Telegram Stars для оплаты PRO
- [ ] AI верификация выполнения квестов (screenshot analysis)
- [ ] Социальные лидерборды
- [ ] Стрики (streak system)
- [ ] Достижения и бейджи
- [ ] Референсная программа
- [ ] Аналитика и A/B тесты

## 📞 Поддержка

Если возникли проблемы:
1. Проверь логи: `sudo supervisorctl tail -f backend` и `sudo supervisorctl tail -f frontend`
2. Проверь статус: `sudo supervisorctl status`
3. Перезапусти сервисы: `sudo supervisorctl restart all`

## 📄 Лицензия

MIT License

---

**Создано с ❤️ для геймификации жизни!**
